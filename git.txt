ls: xem thư mục 
git clone: lấy project về
mkdir: tạo file
git config --global user.name "Rol_le" : cấu hình tên
git status: kiểm tra trạng thái
git add chọn file muốn đưa trên git hub
git commit -m "(message)" : add message (bắt buộc).
git log --oneline : xem lịch sử push

write code  -->  add --> commit --> push